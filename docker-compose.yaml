version: "3.3"

services:
  server:
    container_name: small-chat-server
    build:
      context: .
      dockerfile: ./server/Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./server:/app
